# Retired visualization record

The Gap Matrix, Friction Index and technical-capacity graph are retired in all environments. Their implementation, synthetic trajectories, weights and generators were removed; Git commit `8b0733b` preserves the earlier application. They must not be revived as observed measurements.

`data/sources.json` preserves the essay bibliography and notes at retirement, for documentary comparison. The earlier Gap Matrix research scaffold is in `../../gap-matrix/`; it is a methodological reference only and is never a build input. The institutional map v0.1 sources and methodology remain in `research/releases/v0.1/` at repository root.
